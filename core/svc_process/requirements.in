# svc-process -- Dependencies. Mirrors core/svc_ingest/requirements.in
# (same stage-runner shape, one stage over), plus the DB deps `core/
# svc_action/requirements.in` already carries -- svc-process gained its own
# `AsyncDAL` (config.py/app.py) as of the App Bundle bundle-runtime freeze
# (docs/APP_BUNDLE_AUTHORING.md, 'Accessing the database / shared state').

# Core Framework
quart>=0.19.0
hypercorn>=0.16.0

# Database (flask_core.AsyncDAL wraps pydal directly)
pydal>=20240906.1,<20250101
psycopg2-binary>=2.9.9

# penguin-dal (App Bundle DB access, D21a) -- async Postgres via asyncpg;
# aiosqlite is test-only (in-memory bundle test harness, penguin-dal's own
# tests/test_async_db.py fixture pattern).
penguin-dal==0.4.0
asyncpg==0.30.0
aiosqlite==0.20.0

# HTTP client (distribution API poll)
httpx>=0.27.0,<0.28.0

# Valkey/Redis (LPUSH/RPUSH per-bundle isolation keys)
redis>=5.0.1
hiredis>=2.3.2

# Environment
python-dotenv>=1.0.0

# Development / test
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
fakeredis>=2.21.0
