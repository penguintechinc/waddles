schema_version: 2
app_id: waddles.core.example.echo
name: Echo Example Bundle (JS/TS)
version: 1.0.0
feature: waddles.core.example
module: core
provider: builtin
language: typescript
artifact: source
execution_model: native
is_default: false
stages:
  process:
    entry: "bundle:transform"
    consumes:
      - platform: discord
        event_types: ["chat.message"]
        filters:
          command_prefix: ["!echo"]
    produces: []
    config: {}
    spec:
      required_config: []
  action:
    entry: "bundle:dispatch"
    config: {}
    spec:
      required_config: []
egress: []
data:
  tables: []
limits:
  timeout_ms: 2000
  memory_mb: 64
  egress_rps: 10
permissions: []
config_schema: {}
compatible_with: []
incompatible_with: []
platform_compatibility:
  tested_with: "1.0.0"
  min_version: "1.0.0"
  max_version: null
